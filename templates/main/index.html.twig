{% extends 'base.html.twig' %}

{% block title %}Home | {{ parent() }}{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-body">
            <p class="text-center">Voici des lieux incontournables : </p>
            <ul class="list-group list-group-flush">
                {% for pays, activite in tableau_lieux %}
                    <li class="list-group-item">{{ pays }} : {{ activite }}</li>
                {% endfor %}
            </ul>
        </div>
    </div>


{% endblock %}
